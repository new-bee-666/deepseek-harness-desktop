# 更新日志

## v0.3.0（2026-08-16）

### 新增功能

#### 多模型余额显示

- 对话界面左下角新增 💳 余额按钮，点击展开面板，支持查询：
  - **DeepSeek**：实时余额（CNY）
  - **Kimi**：实时余额（CNY）
  - **OpenAI**：实时余额（USD）
  - **Claude**：本月用量（需 `sk-ant-admin` 管理密钥，非余额）
- 已配置 key 的服务显示实时余额/用量，未配置 key 的显示“未配置”。
- 无公开查询接口的模型不再出现在列表中。
- 面板每 60 秒自动刷新一次。
- 数据读取本地 `.credentials.yaml`，与客户端实际调用所用的 key 一致。

#### 打开当前文件夹

- 对话界面左下角新增 📂 打开文件夹 按钮。
- 点击后用资源管理器打开当前工作区文件夹，与 Codex 客户端的 Open in 行为一致。
- 自动匹配当前会话所在工作区。

#### 关闭窗口驻留系统托盘

- 点击窗口关闭按钮后程序不再退出，而是隐藏到电脑右下角系统托盘（鲸鱼图标），进程继续在后台运行。
- 单击/双击托盘图标恢复窗口。
- 托盘右键菜单提供“显示主窗口”和“退出”。
- 窗口隐藏时再次运行 exe 也会自动恢复窗口。

### 界面调整

- 更换背景入口移入 **设置 → 通用设置**（“选择图片…”/“清除背景”），对话界面只保留余额显示。
- 余额按钮位置上移，避免遮挡底部设置区域。

### 修复与优化

- 修复余额面板颜色显示无效的问题。
- 发布脚本增强：自动创建 `v0.3.0` Release，上传前先删除同名旧附件，避免同名上传不覆盖。

### 版本与产物

- 版本号由 `0.1.0-rc.5` 升级为 `0.3.0`。
- 便携版：`DeepSeek Harness 0.3.0.exe`（约 128 MB，免安装直接运行）。
- 安装版：`DeepSeek Harness Setup 0.3.0.exe`（NSIS 安装程序，可选安装目录、创建桌面快捷方式）。
